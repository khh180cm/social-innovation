# 미래내일 일경험 운영 판단 도구

미취업 청년 일경험 사업의 운영자가 참여자 300명의 상태를 매주 빠르게 보고 판단하도록 돕는 웹 도구. AX Arena 과제로 만든 프로토타입이다. 고용노동부·대한상공회의소 미래내일 일경험 사업을 대상으로 하고, 제공된 더미 데이터(`participants.csv`, 300명 × 8주)로 동작한다.

## 무엇을 하나

운영자가 매주 던지는 네 가지 질문에 한 화면으로 답한다.

- 누가 위험한가 — 출석·수당 미달 위험을 긴급도로 분류
- 왜 그런가 — 주차별 피드백(교사·외부기관·알림톡·구글폼)
- 무엇을 할까 — 개인별 조치와 안내 메시지 초안 추천
- 끝까지 가면 — 남은 주차 출석률·수당 미지급 확률 예측

화면은 세 단계로 이어진다. (1) 예측 필터로 남은 주를 추정하고, (2) 긴급도×경향 12유형으로 분류해 명단을 좁히고, (3) 개인을 열면 AI가 조치를 추천한다.

## 예측 방식

업로드된 주차만 관측으로 보고 남은 주는 예측한다. Discounted Beta-Binomial에 같은 프로그램 코호트 기반 prior를 얹는다. 데이터가 적은 초기에는 코호트 평균 쪽으로 보정하고, 주차가 쌓이면 개인 데이터로 좁혀간다(부분 풀링). 최근 주에 가중(λ=0.8)을 둬 추세 변화를 반영하고, 미지급 확률과 신용구간을 함께 낸다.

수치 예측은 출석 데이터만 쓴다. 피드백 데이터와 LLM(3단계)은 조치 추천·설명에만 쓰고, 수당·확률 같은 수치는 통계 엔진이 결정론적으로 산출한다. 금전과 직결되는 값을 재현·검증 가능하게 두기 위한 분리다.

## 실행

3단계 조치 추천에 Anthropic API 키가 필요하다.

```
cd web
npm install
echo "ANTHROPIC_API_KEY=sk-ant-..." > .env.local
npm run dev
```

http://localhost:3000 에서 확인. 기술 스택은 Next.js 16, React 19, TypeScript, Tailwind, Anthropic SDK.

## 구조

```
participants.csv     제공 더미 데이터 (300명 × 8주)
docs/                PRD · AI 활용 방법론 · 발표 자료(DECK.md, deck.html)
web/src/lib/         예측 엔진(bayes, asof), 더미 피드백 생성, 분류 규칙
web/src/components/   보드 · 12유형 매트릭스 · 상세 모달
web/src/app/api/     조치 추천 API
```

## 데이터

`participants.csv` 컬럼:

| 컬럼 | 설명 |
|------|------|
| `program_name` | 사업유형·운영기관 (일경험_A~E, 직무훈련_A~E, 각 30명) |
| `participant_name` | 참여자명 |
| `dummy_birth_gender_code` | 생년월일+성별코드 (더미) |
| `week_NN_{job_training,work_experience}_{attended,total}_hours` | 주차별 트랙별 출석/총 시간 |

수당은 4주마다 출석률 구간에 따라 차등 지급한다.

| 구간 | 90%+ | 70–90% | 60–70% | 50–60% | <50% |
|------|------|--------|--------|--------|------|
| 직무훈련 (만점 150,000) | 150,000 | 131,250 | 112,500 | 93,750 | 미지급 |
| 일경험 (만점 300,000) | 300,000 | 262,500 | 225,000 | 187,500 | 미지급 |

## 한계

데이터가 더미라 예측 수치는 로직 검증용이며 실제 운영 성능을 보장하지 않는다. 실데이터 적용 시 prior와 추세 구조를 다시 추정해야 한다.
